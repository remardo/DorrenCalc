<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# DORREN Door Calculator - Веб-приложение для расчета стоимости дверей

Полнофункциональное веб-приложение для конфигурации и расчета стоимости дверей с генерацией PDF и облачным хранением данных.

## Функционал

- 🏗️ Конфигуратор дверей с выбором типа, полотна, короба, опций и фурнитуры
- 💰 Автоматический расчет стоимости с поддержкой скидок
- 📄 Генерация коммерческих предложений в PDF формате
- ☁️ Облачное хранение данных через Convex
- 📋 Управление проектами и шаблонами
- 💾 Система черновиков для временного сохранения

## Технологии

- **Frontend**: React 18 + TypeScript + Vite
- **Стилизация**: Кастомный CSS с темной темой
- **База данных**: Convex (облачное хранение)
- **PDF**: jsPDF с поддержкой кириллицы
- **Иконки**: Lucide React

## Запуск проекта

**Предварительные требования:** Node.js

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Настройте переменные окружения в [.env.local](.env.local):
   ```
   CONVEX_URL=ваш_convex_url
   ```

3. Запустите приложение:
   ```bash
   npm run dev
   ```

4. Для работы с Convex в отдельном терминале:
   ```bash
   npx convex dev
   ```

## Структура проекта

- `src/` - клиентский код приложения
  - `convexAdapter.ts` - хуки для работы с Convex
  - `importPriceData.ts` - утилиты для импорта прайс-листа
  - `PriceImporter.tsx` - компонент для импорта прайс-листа
  - `ProductsExample.tsx` - пример работы с продуктами
- `convex/` - серверная логика и схема данных
  - `schema.ts` - схема базы данных
  - `functions.ts` - серверные функции (queries и mutations)
  - `USAGE.md` - подробная документация по использованию
- `components/` - React компоненты
- `fonts/` - локальные шрифты для PDF

## Хранение данных в Convex

Проект использует Convex для хранения:
- **Проектов** - полные конфигурации с расчетами
- **Шаблонов** - сохраненные конфигурации для повторного использования
- **Черновиков** - автосохранение текущего состояния
- **Прайс-листа** - все продукты с ценами (полотна, коробки, опции, фурнитура, аксессуары)

Подробнее см. [QUICKSTART.md](QUICKSTART.md) и [convex/USAGE.md](convex/USAGE.md).

## Развертывание

Проект готов к развертыванию на любой платформе, поддерживающей статические сайты (Vercel, Netlify, GitHub Pages).

Сборка проекта:
```bash
npm run build
```
