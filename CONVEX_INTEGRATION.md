# Сохранение проектов и шаблонов в Convex DB

## Обзор

В проекте DORREN Door Calculator реализовано полноценное сохранение проектов и шаблонов в базе данных Convex. Это обеспечивает:

- **Надежное хранение** - данные сохраняются в облачной БД Convex
- **Синхронизация** - доступ к данным с любого устройства
- **Резервное копирование** - автоматическое резервное копирование
- **Масштабируемость** - поддержка множественных пользователей

## Архитектура

### Схема базы данных

```typescript
// convex/schema.ts
export default defineSchema({
  // Проекты пользователей
  projects: defineTable({
    name: v.string(),
    customer: v.string(),
    manager: v.string(),
    comments: v.string(),
    items: v.array(v.object({...})), // элементы проекта
    totalAmount: v.number(),
    createdAt: v.number(),
    updatedAt: v.number(),
  }).index("by_createdAt", ["createdAt"]),

  // Шаблоны конфигураций
  templates: defineTable({
    name: v.string(),
    config: v.object({...}), // конфигурация шаблона
    createdAt: v.number(),
  }).index("by_createdAt", ["createdAt"]),

  // Черновики
  drafts: defineTable({
    config: v.object({...}), // текущая конфигурация
    createdAt: v.number(),
  }).index("by_createdAt", ["createdAt"]),
});
```

### Доступные функции

#### Для проектов:
- `saveProject()` - сохранение проекта
- `getProjects()` - получение всех проектов
- `deleteProject()` - удаление проекта

#### Для шаблонов:
- `saveTemplate()` - сохранение шаблона
- `getTemplates()` - получение всех шаблонов
- `deleteTemplate()` - удаление шаблона
- `updateTemplate()` - обновление шаблона

#### Для черновиков:
- `saveDraft()` - сохранение черновика
- `getDraft()` - получение черновика
- `deleteDraft()` - удаление черновика

## Использование в компонентах

### React хуки

```typescript
import { useProjects, useTemplates, useDraft } from './convexAdapter';

// Получение данных
const { projects, saveProject, deleteProject } = useProjects();
const { templates, saveTemplate, deleteTemplate } = useTemplates();
const { draft, saveDraft, deleteDraft } = useDraft();
```

### Сохранение проекта

```typescript
const handleSaveProject = async () => {
  const projectData = {
    name: "Мой проект",
    customer: "ООО Компания",
    manager: "Иван Иванов",
    comments: "Комментарии к проекту",
    items: [...projectItems], // массив элементов проекта
    totalAmount: 150000
  };

  try {
    await saveProject(projectData);
    console.log('Проект сохранен в Convex');
  } catch (error) {
    console.error('Ошибка сохранения:', error);
  }
};
```

### Сохранение шаблона

```typescript
const handleSaveTemplate = async () => {
  const templateData = {
    name: "Стандартная офисная дверь",
    config: {
      doorType: "single",
      leaf: selectedLeaf,
      frame: selectedFrame,
      options: selectedOptions,
      hardware: selectedHardware,
      accessories: selectedAccessories
    }
  };

  try {
    await saveTemplate(templateData);
    console.log('Шаблон сохранен в Convex');
  } catch (error) {
    console.error('Ошибка сохранения шаблона:', error);
  }
};
```

## Особенности реализации

### Двойное хранение

Система использует **двойное хранение**:
1. **LocalStorage** - для быстрого доступа и работы офлайн
2. **Convex DB** - для надежного облачного хранения

Это обеспечивает:
- Работу без интернета (localStorage)
- Синхронизацию между устройствами (Convex)
- Резервное копирование (Convex)

### Индикатор статуса

В интерфейсе добавлен индикатор подключения к Convex:
- 🟢 Зеленая точка - Convex подключен и работает
- Отображается "DB Active" в заголовке

### Обработка ошибок

Все операции с Convex обернуты в try-catch блоки:
```typescript
try {
  await saveProject(projectData);
  console.log('Сохранено в Convex');
} catch (error) {
  console.error('Ошибка Convex:', error);
  // Пользователь может продолжить работу с localStorage
}
```

## Тестирование

Для тестирования функций Convex используйте компонент `ConvexTest`:

```typescript
// Добавьте в App.tsx для тестирования
import ConvexTest from './src/ConvexTest';

// В JSX
<ConvexTest />
```

## Мониторинг

В консоли браузера можно отслеживать операции:
- `Проект сохранен в Convex` - успешное сохранение проекта
- `Шаблон сохранен в Convex` - успешное сохранение шабона
- `Черновик сохранен в Convex` - успешное сохранение черновика
- `Ошибка сохранения в Convex:` - ошибки сохранения

## Преимущества

1. **Надежность** - данные не потеряются при очистке браузера
2. **Доступность** - можно работать с разных устройств
3. **Производительность** - быстрые запросы к БД
4. **Масштабируемость** - поддержка множественных пользователей
5. **Безопасность** - автоматическое резервное копирование

## Будущие улучшения

- Синхронизация между вкладками браузера
- Экспорт/импорт данных
- Версионирование проектов
- Комментирование и тегирование
- Поиск по проектам и шаблонам